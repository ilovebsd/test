package command;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import com.acromate.util.Str;
import dao.useconfig.AddServiceDAO;

public class SendPrefixEditCommand implements Command {
	public String execute(HttpServletRequest req, HttpServletResponse res) {
		String forwardingPage = "";

        int 	nGroupId = 0;
        boolean returnVal 	= false;
        try {
			String 	hiBlockNumber	= new String(req.getParameter("hiBlockNumber").getBytes("8859_1"), "euc-kr");			// 이름
			String 	hiDescription	= new String(req.getParameter("hiDescription").getBytes("8859_1"), "euc-kr");			// 이름
			
			AddServiceDAO dao = new AddServiceDAO();
			
			// 사용자 정보 수정
			returnVal = dao.sendPrefixEdit(hiBlockNumber, hiDescription);

			if (returnVal) {
				System.out.println("발신제한 번호 수정 성공 .......");
				//forwardingPage = "/bizportal/publicAddrList.jsp?group=" + nGroupId + "&result=" + Integer.toString(updateResult);
				res.sendRedirect("/bizportal/useconfig/addServiceList.jsp");
			}else{
				System.out.println("발신제한 번호 수정 실패 .......");
				//forwardingPage = "/address/publicGroupList.jsp?session=0";
				res.sendRedirect("/bizportal/errorMessage.jsp");
			}
			
		} catch (Exception e) {
			System.out.println("발신제한 번호 에러발생 .......");
			try{
				res.sendRedirect("/bizportal/errorMessage.jsp");
			} catch (Exception ee) {
			}
			
			e.printStackTrace();
		}		
		return forwardingPage;
	}
}
