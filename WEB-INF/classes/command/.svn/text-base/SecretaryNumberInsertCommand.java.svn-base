package command;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import com.acromate.util.Str;
import dao.useconfig.AddServiceDAO;

public class SecretaryNumberInsertCommand implements Command {
	public String execute(HttpServletRequest req, HttpServletResponse res) {
		String 	forwardingPage 	= "";
		boolean returnVal 		= false;

		try {			
			String 	insertStr 		= new String(Str.CheckNullString(req.getParameter("insertStr")).getBytes("8859_1"), "euc-kr");	// 
			String 	userparam 		= new String(Str.CheckNullString(req.getParameter("hiNumber")).getBytes("8859_1"), "euc-kr");	// 비서 번호

			String 	userID			= new String(req.getParameter("hiUserID").getBytes("8859_1"), "euc-kr");		// 로그인 ID
			
			AddServiceDAO dao 	= new AddServiceDAO();
			
			// ############### LogHistory 처리  ###############
			//returnVal = dao.secretaryNumberInsert(insertStr, userparam);
			returnVal = dao.secretaryNumberInsert(insertStr, userparam, req.getRemoteAddr(), userID);
			// ##############################################
			
			if (returnVal) {
				res.sendRedirect("/bizportal/useconfig/addServiceSecretaryList.jsp");
			}else{
				res.sendRedirect("/bizportal/errorMessage.jsp");
			}			
		
		} catch (Exception e) {
			try{
				res.sendRedirect("/bizportal/errorMessage.jsp");
			} catch (Exception ee) {
			}
			e.printStackTrace();
		} finally {
			
			
		}
		return forwardingPage;
	}
}
