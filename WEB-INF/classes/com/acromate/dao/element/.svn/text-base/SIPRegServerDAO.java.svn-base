package com.acromate.dao.element;

/**
 * <p>Title: </p>
 *
 * <p>Description: </p>
 *
 * <p>Copyright: Copyright (c) 2007</p>
 *
 * <p>Company: </p>
 *
 * @author not attributable
 * @version 1.0
 */
import com.acromate.db.QueryStatement ;
import java.util.ArrayList;
import com.acromate.util.Str;
import java.sql.ResultSet;
import com.acromate.dto.element.SIPRegServerDTO;
import com.acromate.util.Logger;

public class SIPRegServerDAO {
    private Logger log = Logger.getLogger(this.getClass());

    private SIPRegServerDTO sipRegServerDTO ;
    QueryStatement statement ;

    public SIPRegServerDAO() {}

    public SIPRegServerDAO( QueryStatement statement, SIPRegServerDTO sipRegServerDTO ) {
        this.statement = statement ;
        this.sipRegServerDTO = sipRegServerDTO ;
    }

    public ArrayList getList(QueryStatement statement, String SubID) {
        ArrayList list = new ArrayList() ;
        SIPRegServerDTO sipRegServerDTO = null ;
        ResultSet rs = null ;

        try {
            String queryString = "SELECT subID, serverIP, serverPort, serverTransport, userDomain, userAuthID, userAuthPasswd, expireTime " ;
            queryString += " From Table_SIPRegServer Where subID = '" + SubID + "' ";

            rs = statement.executeQuery(queryString) ;
            log.info(queryString) ;
/*
            while ( rs.next() )  {
                sipRegServerDTO = new SIPRegServerDTO() ;
                sipRegServerDTO.setSubID(Str.CheckNullString(rs.getString(1));
                sipRegServerDTO.setServerIP(Str.CheckNullString(rs.getString(2));
                sipRegServerDTO.setServerPort(rs.getInt(3));
                sipRegServerDTO.setServerTransport(rs.getInt(4));
                sipRegServerDTO.setUserDomain(Str.CheckNullString(rs.getString(5));
                sipRegServerDTO.setUserAuthID(Str.CheckNullString(rs.getString(6));
                sipRegServerDTO.setUserAuthPasswd(Str.CheckNullString(rs.getString(7));
                sipRegServerDTO.setExpireTime(rs.getInt(8));
                list.add(sipRegServerDTO) ;
            }*/
        } catch(Exception e) {
            log.error("exception occurred > ", e);
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { log.error("exception occurred > ", e); }
        }

        return list;
    }

    public boolean insert ( QueryStatement statement, SIPRegServerDTO sipRegServerDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString =   " INSERT INTO Table_SIPRegServer ";
            queryString += " (subID, serverIP, serverPort, serverTransport, userDomain, userAuthID, userAuthPasswd, expireTime )";
            queryString += " VALUES (" ;
            queryString += " '" + sipRegServerDTO.getSubID() + "' ," ;
            queryString += " '" + sipRegServerDTO.getServerIP() + "'," ;
            queryString += "  " + sipRegServerDTO.getServerPort() + "," ;
            queryString += "  " + sipRegServerDTO.getServerTransport() + "," ;
            queryString += " '" + sipRegServerDTO.getUserDomain() + "'," ;
            queryString += " '" + sipRegServerDTO.getUserAuthID() + "'," ;
            queryString += " '" + sipRegServerDTO.getUserAuthPasswd() + "'," ;
            queryString += " " + sipRegServerDTO.getExpireTime() + ") " ;

            log.info(queryString) ;
            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }


    public boolean update( QueryStatement statement, SIPRegServerDTO sipRegServerDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString = " UPDATE Table_SIPRegServer SET ";
            queryString += "serverIP =  " + sipRegServerDTO.getServerIP() + "," ;
            queryString += "serverPort =  " + sipRegServerDTO.getServerPort() + "," ;
            queryString += "serverTransport =  " + sipRegServerDTO.getServerTransport() + "," ;
            queryString += "userDomain =  '" + sipRegServerDTO.getUserDomain() + "'," ;
            queryString += "userAuthID =  '" + sipRegServerDTO.getUserAuthID() + "'," ;
            queryString += "userAuthPasswd =  '" + sipRegServerDTO.getUserAuthPasswd() + "'," ;
            queryString += "expireTime = " + sipRegServerDTO.getSubID() + " " ;
            queryString += " Where subID = '" + sipRegServerDTO.getSubID() + "' ";

            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }

    public boolean delete( QueryStatement statement,  SIPRegServerDTO sipRegServerDTO) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_SIPRegServer Where subID = '" + sipRegServerDTO.getSubID() + "' ";

            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }
}





