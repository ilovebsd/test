package com.acromate.dao.element;

import com.acromate.db.QueryStatement ;
import java.util.ArrayList;
import com.acromate.util.Str;
import java.sql.ResultSet;
import com.acromate.dto.element.GroupPrefixDTO;


public class GroupPrefixDAO {

    private GroupPrefixDTO groupPrefixDTO ;
    QueryStatement statement ;

    public GroupPrefixDAO() {}

    public GroupPrefixDAO( QueryStatement statement, GroupPrefixDTO groupPrefixDTO ) {
        this.statement = statement ;
        this.groupPrefixDTO = groupPrefixDTO ;
    }

    public GroupPrefixDTO getDTO(QueryStatement statement, String strStartPrefix ) {
        GroupPrefixDTO groupPrefixDTO = null;
        ResultSet rs = null;

        try {
            String queryString = "SELECT startPrefix, endPrefix, groupID, subID  ";
            queryString += " From Table_GroupPrefix ";
            queryString += " WHERE startPrefix = '" + strStartPrefix + "' ";
            System.out.println(queryString) ;
            rs = statement.executeQuery(queryString) ;

            if ( rs.next() )  {
                groupPrefixDTO = new GroupPrefixDTO() ;

                groupPrefixDTO.setStartPrefix(Str.CheckNullString(rs.getString(1)));
                groupPrefixDTO.setEndPrefix(Str.CheckNullString(rs.getString(2)));
                groupPrefixDTO.setGroupID(Str.CheckNullString(rs.getString(3)));
                groupPrefixDTO.setSubID(Str.CheckNullString(rs.getString(4)));
            }
        } catch(Exception e) {
            e.printStackTrace();
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { e.printStackTrace(); }
        }
        return groupPrefixDTO;
    }


    public ArrayList getList(QueryStatement statement) {
        ArrayList list = new ArrayList();
        GroupPrefixDTO groupPrefixDTO = null;
        ResultSet rs = null;

        try {
            String queryString = "SELECT startPrefix, endPrefix, groupID, subID  ";
            queryString += " From Table_GroupPrefix ";
            System.out.println(queryString) ;
            rs = statement.executeQuery(queryString) ;

            while ( rs.next() )  {
                groupPrefixDTO.setStartPrefix(Str.CheckNullString(rs.getString(1)));
                groupPrefixDTO.setEndPrefix(Str.CheckNullString(rs.getString(2)));
                groupPrefixDTO.setGroupID(Str.CheckNullString(rs.getString(3)));
                groupPrefixDTO.setSubID(Str.CheckNullString(rs.getString(4)));

                list.add(groupPrefixDTO) ;
            }
        } catch(Exception e) {
            e.printStackTrace();
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { e.printStackTrace(); }
        }

        return list;
    }

    public boolean insert ( QueryStatement statement, GroupPrefixDTO groupPrefixDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString =   " INSERT INTO Table_GroupPrefix ";
            queryString += " ( startPrefix, endPrefix, groupID, subID) ";
            queryString += " VALUES (" ;

            queryString += " '"+groupPrefixDTO.getStartPrefix()+"', ";
            queryString += " '"+groupPrefixDTO.getEndPrefix()+"', ";
            queryString += " '"+groupPrefixDTO.getGroupID()+"', ";
            queryString += " '"+groupPrefixDTO.getSubID()+"') ";

            System.out.println(queryString) ;
            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }

    public boolean update( QueryStatement statement, GroupPrefixDTO groupPrefixDTO  ) {
    String queryString = "";
    int count = 0;
    try {
        if (statement == null) {
            return false;
        }

        queryString = " UPDATE Table_GroupPrefix SET ";

        queryString += " endPrefix = '"+ groupPrefixDTO.getEndPrefix()+"', ";
        queryString += " groupID = '"+ groupPrefixDTO.getGroupID()+"', ";
        queryString += " subID = '"+ groupPrefixDTO.getSubID()+"' ";
        queryString += " WHERE StartPrefix = '" + groupPrefixDTO.getStartPrefix() +"' ";


        count = statement.executeUpdate(queryString);
        System.out.println(queryString) ;
    } catch (Exception e) {
        e.printStackTrace();
        return false;
    }
    return true ;
}

    public boolean delete( QueryStatement statement,  GroupPrefixDTO groupPrefixDTO) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_GroupPrefix WHERE startPrefix = '" + groupPrefixDTO.getStartPrefix()+ "' " ;
            count = statement.executeUpdate(queryString);
            System.out.println(queryString) ;
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }

    public boolean deleteAll( QueryStatement statement ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_GroupPrefix " ;
            count = statement.executeUpdate(queryString);
            System.out.println(queryString) ;
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }
}
