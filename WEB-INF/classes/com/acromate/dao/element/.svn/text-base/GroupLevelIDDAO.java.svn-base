package com.acromate.dao.element;

import com.acromate.db.QueryStatement ;
import java.util.ArrayList;
import com.acromate.util.Str;
import java.sql.ResultSet;
import com.acromate.dto.element.GroupLevelIDDTO;

public class GroupLevelIDDAO {

    private GroupLevelIDDTO groupLevelIDDTO ;
    QueryStatement statement ;

    public GroupLevelIDDAO() {}

    public GroupLevelIDDAO( QueryStatement statement, GroupLevelIDDTO groupLevelIDDTO ) {
        this.statement = statement ;
        this.groupLevelIDDTO = groupLevelIDDTO ;
    }

    public GroupLevelIDDTO getDTO(QueryStatement statement, String strGroupID, String strLevelID ) {
        GroupLevelIDDTO groupLevelIDDTO = null;
        ResultSet rs = null;

        try {
            String queryString = "SELECT groupID, levelID, description  ";
            queryString += " From Table_LevelID ";
            queryString += " WHERE groupID='" + strGroupID + "' ";
            queryString += " AND levelID = '" + strLevelID + "' " ;
            System.out.println(queryString) ;
            rs = statement.executeQuery(queryString) ;

            if ( rs.next() )  {
                groupLevelIDDTO = new GroupLevelIDDTO() ;

                groupLevelIDDTO.setGroupID(Str.CheckNullString(rs.getString(1)));
                groupLevelIDDTO.setLevelID(Str.CheckNullString(rs.getString(2)));
                groupLevelIDDTO.setDescription(Str.CheckNullString(rs.getString(3)));
            }
        } catch(Exception e) {
            e.printStackTrace();
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { e.printStackTrace(); }
        }
        return groupLevelIDDTO;
    }


    public ArrayList getList(QueryStatement statement) {
        ArrayList list = new ArrayList();
        GroupLevelIDDTO groupLevelIDDTO = null;
        ResultSet rs = null;

        try {
            String queryString = "SELECT groupID, levelID, description  ";
            queryString += " From Table_LevelID ";
            System.out.println(queryString) ;
            rs = statement.executeQuery(queryString) ;

            while ( rs.next() )  {
                groupLevelIDDTO.setGroupID(Str.CheckNullString(rs.getString(1)));
                groupLevelIDDTO.setLevelID(Str.CheckNullString(rs.getString(2)));
                groupLevelIDDTO.setDescription(Str.CheckNullString(rs.getString(3)));

                list.add(groupLevelIDDTO) ;
            }
        } catch(Exception e) {
            e.printStackTrace();
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { e.printStackTrace(); }
        }

        return list;
    }

    public boolean insert ( QueryStatement statement, GroupLevelIDDTO groupLevelIDDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString =   " INSERT INTO Table_LevelID ";
            queryString += " ( groupID, levelID, description) ";
            queryString += " VALUES (" ;

            queryString += " '"+groupLevelIDDTO.getGroupID()+"', ";
            queryString += " '"+groupLevelIDDTO.getLevelID()+"', ";
            queryString += " '"+groupLevelIDDTO.getDescription()+"') ";

            System.out.println(queryString) ;
            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }

    public boolean update( QueryStatement statement, GroupLevelIDDTO groupLevelIDDTO  ) {
    String queryString = "";
    int count = 0;
    try {
        if (statement == null) {
            return false;
        }

        queryString = " UPDATE Table_LevelID SET ";

        queryString += " Description = '"+ groupLevelIDDTO.getDescription()+"' ";
        queryString += " Where GroupID = '" + groupLevelIDDTO.getGroupID() + "' ";
        queryString += " AND LevelID = '" + groupLevelIDDTO.getLevelID() + "' " ;
        count = statement.executeUpdate(queryString);
        System.out.println(queryString) ;
    } catch (Exception e) {
        e.printStackTrace();
        return false;
    }
    return true ;
    }

    public boolean delete( QueryStatement statement,  GroupLevelIDDTO groupLevelIDDTO) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_LevelID WHERE GroupID = '" + groupLevelIDDTO.getGroupID()+ "' " ;
            queryString += " AND LevelID = '" + groupLevelIDDTO.getLevelID() + "' " ;
            count = statement.executeUpdate(queryString);
            System.out.println(queryString) ;
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }

    public boolean deleteAll( QueryStatement statement ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_LevelID " ;
            count = statement.executeUpdate(queryString);
            System.out.println(queryString) ;
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }
}
