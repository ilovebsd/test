package com.acromate.dao.element;

/**
 * <p>Title: </p>
 *
 * <p>Description: </p>
 *
 * <p>Copyright: Copyright (c) 2007</p>
 *
 * <p>Company: </p>
 *
 * @author not attributable
 * @version 1.0
 */
import com.acromate.db.QueryStatement ;
import java.util.ArrayList;
import com.acromate.util.Str;
import java.sql.ResultSet;
import com.acromate.dto.element.SipAuthAliasDTO;
import com.acromate.util.Logger;

public class SipAuthAliasDAO {
    private Logger log = Logger.getLogger(this.getClass());

    private SipAuthAliasDTO sipAuthAliasDTO ;
    QueryStatement statement ;

    public SipAuthAliasDAO() {}

    public SipAuthAliasDAO( QueryStatement statement, SipAuthAliasDTO sipAuthAliasDTO ) {
        this.statement = statement ;
        this.sipAuthAliasDTO = sipAuthAliasDTO ;
    }

    public ArrayList getList(QueryStatement statement, String EndpointID, int AliasID) {
        ArrayList list = new ArrayList() ;
        SipAuthAliasDTO sipAuthAliasDTO = null ;
        ResultSet rs = null ;

        try {
            String queryString = "SELECT  endpointID, aliasID " ;
            queryString += " From Table_SipAuthAlias Where endpointID = '" + EndpointID +"' ";
            queryString += " AND AliasID = '" + AliasID + "' ";
            rs = statement.executeQuery(queryString) ;
            log.info(queryString) ;
/*
            while ( rs.next() )  {
                sipAuthAliasDTO = new SipAuthAliasDTO() ;
                sipAuthAliasDTO.setEndpointID(Str.CheckNullString(rs.getString(1));
                sipAuthAliasDTO.setAliasID(Str.CheckNullString(rs.getString(2));
                list.add(sipAuthAliasDTO) ;
            }*/
        } catch(Exception e) {
            log.error("exception occurred > ", e);
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { log.error("exception occurred > ", e); }
        }

        return list;
    }

    public int getAliasCount(QueryStatement statement, String endpointID) {
        ArrayList list = new ArrayList() ;
        ResultSet rs = null ;
        int count = 0;
        try {
            String queryString = "SELECT count(*) FROM Table_SipAuthAlias WHERE Endpointid = '"+endpointID+"'";
            System.out.println(queryString) ;
            rs = statement.executeQuery(queryString) ;
            if (rs.next())
                count = rs.getInt(1);
            rs.close();
        } catch(Exception e) {
            e.printStackTrace();
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { e.printStackTrace(); }
        }
        return count;
    }

    public int getAliasCount(QueryStatement statement, String endpointID, String aliasID) {
        ArrayList list = new ArrayList() ;
        ResultSet rs = null ;
        int count = 0;
        try {
            String queryString = "SELECT count(*) FROM Table_SipAuthAlias WHERE Endpointid = '"+endpointID+"' AND  aliasid = '" + aliasID + "' ";
            System.out.println(queryString) ;
            rs = statement.executeQuery(queryString) ;
            if (rs.next())
                count = rs.getInt(1);
            rs.close();
        } catch(Exception e) {
            e.printStackTrace();
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { e.printStackTrace(); }
        }
        return count;
    }

    public boolean insert ( QueryStatement statement, SipAuthAliasDTO sipAuthAliasDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString =   " INSERT INTO Table_SipAuthAlias ";
            queryString += " (endpointID, aliasID)";
            queryString += " VALUES (" ;
            queryString += " '" + sipAuthAliasDTO.getEndpointID() + "' ," ;
            queryString += " '" + sipAuthAliasDTO.getAliasID() + "') " ;

            log.info(queryString) ;
            count = statement.executeUpdate(queryString);
            if (count < 1) return false ;
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }


    public boolean update( QueryStatement statement, SipAuthAliasDTO sipAuthAliasDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString = " UPDATE Table_SipAuthAlias SET ";
            queryString += "endpointID = '" + sipAuthAliasDTO.getEndpointID() + "' ," ;
            queryString += "aliasID = '" + sipAuthAliasDTO.getAliasID() + "' " ;
            queryString += " WHERE endpointID = '" + sipAuthAliasDTO.getEndpointID() + "' ";
            queryString += " AND aliasID = '" + sipAuthAliasDTO.getAliasID() + "' " ;


            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }

    public boolean delete( QueryStatement statement,  String endpointID) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_SipAuthAlias WHERE endpointID = '" + endpointID + "' ";
            count = statement.executeUpdate(queryString);
            System.out.println(count + " deleted. " + queryString) ;
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }

    public boolean delete( QueryStatement statement,  String endpointID, String aliasID) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_SipAuthAlias WHERE endpointID = '" + endpointID + "'  AND aliasid = '" + aliasID + "' ";
            count = statement.executeUpdate(queryString);
            System.out.println(count + " deleted. " + queryString) ;
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
        return true ;
    }

    public boolean delete( QueryStatement statement,  SipAuthAliasDTO sipAuthAliasDTO) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_SipAuthAlias WHERE endpointID = '" + sipAuthAliasDTO.getEndpointID() + "' ";
            queryString += " AND aliasID = '" + sipAuthAliasDTO.getAliasID() + "' " ;
            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }
}


