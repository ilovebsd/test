package com.acromate.dao.element;

/**
 * <p>Title: </p>
 *
 * <p>Description: </p>
 *
 * <p>Copyright: Copyright (c) 2007</p>
 *
 * <p>Company: </p>
 *
 * @author not attributable
 * @version 1.0
 */
import com.acromate.db.QueryStatement ;
import java.util.ArrayList;
import com.acromate.util.Str;
import java.sql.ResultSet;
import com.acromate.dto.element.GroupBlockDTO;
import com.acromate.util.Logger;

public class GroupBlockDAO {
    private Logger log = Logger.getLogger(this.getClass());

    private GroupBlockDTO groupBlockDTO ;
    QueryStatement statement ;

    public GroupBlockDAO() {}

    public GroupBlockDAO( QueryStatement statement, GroupBlockDTO groupBlockDTO ) {
        this.statement = statement ;
        this.groupBlockDTO = groupBlockDTO ;
    }

    public ArrayList getList(QueryStatement statement, String GroupID, int InOutFlag, String StartPrefix) {
        ArrayList list = new ArrayList() ;
        GroupBlockDTO groupBlockDTO = null ;
        ResultSet rs = null ;

        try {
            String queryString = "SELECT groupID, inoutFlag, startPrefix, endPrefix, blockID, prefixType, blockIDType, description " ;
            queryString += " From Table_GroupBlock Where groupID = '" + GroupID + "' ";
            queryString += " AND inoutFlag = " + InOutFlag + " " ;
            queryString += " AND startPrefix = '" + StartPrefix + "' " ;

            rs = statement.executeQuery(queryString) ;
            log.info(queryString) ;
/*
            while ( rs.next() )  {
                groupBlockDTO = new GroupBlockDTO() ;
                groupBlockDTO.setGroupID(Str.CheckNullString(rs.getString(1));
                groupBlockDTO.setInoutFlag(rs.getInt(2));
                groupBlockDTO.setStartPrefix(Str.CheckNullString(rs.getString(3));
                groupBlockDTO.setEndPrefix(Str.CheckNullString(rs.getString(4));
                groupBlockDTO.setBlockID(Str.CheckNullString(rs.getString(5));
                groupBlockDTO.setPrefixType(rs.getInt(6));
                groupBlockDTO.setBlockIDType(rs.getInt(7));
                groupBlockDTO.setDescription(Str.CheckNullString(rs.getString(8));
                list.add(groupBlockDTO) ;
            }*/
        } catch(Exception e) {
            log.error("exception occurred > ", e);
        } finally {
            try {
                if (rs != null) rs.close();
            } catch(Exception e) { log.error("exception occurred > ", e); }
        }

        return list;
    }

    public boolean insert ( QueryStatement statement, GroupBlockDTO groupBlockDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString =   " INSERT INTO Table_GroupBlock ";
            queryString += " (groupID, inoutFlag, startPrefix, endPrefix, blockID, prefixType, blockIDType, description)";
            queryString += " VALUES (" ;
            queryString += " '" + groupBlockDTO.getGroupID() + "' ," ;
            queryString += "  " + groupBlockDTO.getInoutFlag() + "," ;
            queryString += " '" + groupBlockDTO.getStartPrefix() + "'," ;
            queryString += " '" + groupBlockDTO.getEndPrefix() + "'," ;
            queryString += " '" + groupBlockDTO.getBlockID() + "'," ;
            queryString += "  " + groupBlockDTO.getPrefixType() + "'," ;
            queryString += "  " + groupBlockDTO.getBlockIDType() + "'," ;
            queryString += " '" + groupBlockDTO.getDescription() + "') " ;

            log.info(queryString) ;
            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }


    public boolean update( QueryStatement statement, GroupBlockDTO groupBlockDTO  ) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }

            queryString = " UPDATE Table_GroupBlock SET ";
            queryString += "endPrefix = '" + groupBlockDTO.getEndPrefix() + "'," ;
            queryString += "blockID = '" + groupBlockDTO.getBlockID() + "'," ;
            queryString += "prefixType = " + groupBlockDTO.getPrefixType() + "," ;
            queryString += "blockIDType = " + groupBlockDTO.getBlockIDType() + "," ;
            queryString += "description = " + groupBlockDTO.getDescription() + " " ;
            queryString += " Where groupID = '" + groupBlockDTO.getGroupID() + "' ";
            queryString += " AND inoutFlag = " + groupBlockDTO.getInoutFlag() + " " ;
            queryString += " AND startPrefix = '" + groupBlockDTO.getStartPrefix() + "' " ;


            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }

    public boolean delete( QueryStatement statement,  GroupBlockDTO groupBlockDTO) {
        String queryString = "";
        int count = 0;
        try {
            if (statement == null) {
                return false;
            }
            queryString = " DELETE FROM Table_GroupBlock Where groupID = '" + groupBlockDTO.getGroupID() + "' ";
            queryString += " AND inoutFlag = " + groupBlockDTO.getInoutFlag() + " " ;
            queryString += " AND startPrefix = '" + groupBlockDTO.getStartPrefix() + "' " ;

            count = statement.executeUpdate(queryString);
        } catch (Exception e) {
            log.error("exception occurred > ", e);
            return false;
        }
        return true ;
    }
}





